import FormCredentials from '../../components/FormCredentials';

export default function Page() {
  return (
    <section>
      <FormCredentials type="signup" action="Create Account" />
    </section>
  );
}
